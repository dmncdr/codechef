#include<iostream>
#define max_n 100002
using namespace std;
arr[max_n];
double rdf()
int main(){
		int t;
		int n,k;
		cin>>n>>k;
		double ans=rdf(n,0);
		cout<<ans<<endl;
		return 0;}
	double rdf(){
			 if(k==0) return arr[n];
			 else {
			 		for(i=0;i<n;i++){
			 				 arr[n]=rdf(i,k-1);
			 				  
			 		
		
